#include<stdio.h>

int main () {
	
	char grade;
	
	printf("Enter the grade");
	scanf("%c",&grade);
	
	switch(grade) {
		case 'A':
			printf("A grade");
			break;
		case 'B':
			printf("B grade");
			break;
			default:
				printf("invalid grade");
				break;
	}
	getch();
	return 0;
}
